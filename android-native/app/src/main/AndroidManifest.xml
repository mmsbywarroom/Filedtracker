<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- Package visibility so we can detect common VPN / fake-GPS apps on Android 11+ -->
    <queries>
        <intent>
            <action android:name="android.net.VpnService" />
        </intent>
        <package android:name="free.vpn.unblock.proxy.turbovpn" />
        <package android:name="free.vpn.proxy.unblock.turbovpn.pro" />
        <package android:name="com.lexa.fakegps" />
        <package android:name="com.incorporateapps.fakegps" />
        <package android:name="com.blogspot.newapphorizons.fakegps" />
        <package android:name="com.expressvpn.vpn" />
        <package android:name="com.nordvpn.android" />
        <package android:name="com.protonvpn.android" />
        <package android:name="org.outline.android.client" />
        <package android:name="com.v2ray.ang" />
        <package android:name="de.blinkt.openvpn" />
        <package android:name="com.github.shadowsocks" />
        <package android:name="com.psiphon3" />
        <package android:name="com.psiphon3.subscription" />
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:hardwareAccelerated="true"
        android:theme="@style/Theme.AapNative"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AapNative">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Compose host: login, home, map, leave, footprints, face check -->
        <activity
            android:name=".compose.ComposeMainActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize" />

        <!-- Legacy XML screens, kept for reference; not on the launcher path anymore. -->
        <activity android:name=".LoginActivity" android:exported="false" />
        <activity android:name=".DashboardActivity" android:exported="false" />
        <activity
            android:name=".FaceCaptureActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize" />

        <activity
            android:name=".WebShellActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize" />

        <!-- Injected from local.properties (MAPS_API_KEY) or the MAPS_API_KEY env var. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <service
            android:name="in.videh.filedtracker.bglocation.FieldLocationService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <receiver
            android:name="in.videh.filedtracker.bglocation.BootCompletedReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="in.videh.filedtracker.bglocation.IntervalAlarmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="in.videh.filedtracker.INTERVAL_CHECK" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
